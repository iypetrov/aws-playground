---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: elk
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/iypetrov/aws-playground.git
    targetRevision: main
    path: eks-argocd/apps/prod/elk
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: {{ .Values.elkServerEndpoint }}
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=true
      - CreateNamespace=false
      - PrunePropagationPolicy=foreground
      - PruneLast=true
